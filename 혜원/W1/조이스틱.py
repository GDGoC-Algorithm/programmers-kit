# 적용 알고리즘의 개념
    # Greedy 알고리즘
    # 이 문제에서는
        # 알파벳을 바꾸는 상하 이동은 각 글자마다 독립적으로 최소값을 바로 구할 수 있음
        # 좌우 이동은 A가 연속되는 구간을 어떻게 건너뛸지 판단하면서 최소 이동을 찾아야 함

# 문제 풀이를 위한 접근 방식(or 개념) 설명
    # 문제 목표: 이름을 완성하기 위한 조이스틱 조작 횟수의 최솟값을 구해야 함 (min)
    # 조작 종류:
        # 상하 이동: 현재 알파벳을 바꾸는 이동
        # 좌우 이동: 커서를 옮기는 이동
    # 단서:
        # 처음 상태는 전부 A이므로, A인 자리는 굳이 바꿀 필요가 없음
        # 각 알파벳을 만드는 상하 이동 횟수는 독립적으로 계산 가능함
        # 예를 들어 J를 만들 때는 위로 9번, Z를 만들 때는 아래로 1번이 최소이므로
        # 각 글자는 위/아래 중 더 적은 쪽을 선택하면 됨
        # 어려운 부분은 좌우 이동임
        # 단순히 오른쪽으로 끝까지 가는 것만이 최소가 아닐 수 있음
        # 중간에 A가 연속되면, 그 구간은 건너뛰는 것이 유리할 수 있음
        # 따라서 좌우 이동은
            # 계속 오른쪽으로 가는 경우
            # 가다가 다시 왼쪽으로 꺾는 경우
            # 반대로 먼저 왼쪽으로 갔다가 다시 오른쪽으로 도는 경우
          를 비교해야 함

# 기본 코드와 개선한 코드 비교
    # 단순 오른쪽 이동 방식 vs A 구간을 고려한 최소 이동 계산
        # 처음에는 커서를 그냥 오른쪽으로 끝까지 이동하면 된다고 생각할 수 있음
        # 하지만 name 안에 A가 길게 이어지는 구간이 있으면, 그 구간을 굳이 지나가지 않는 편이 더 유리할 수 있음
        # 단순히 len(name) - 1 로 좌우 이동을 고정해버림 
        # 개선 코드는 각 위치마다
            # 여기까지 오른쪽으로 갔다가 되돌아오는 경우
            # 반대로 뒤쪽부터 먼저 처리하는 경우
          를 모두 비교해서 가장 작은 좌우 이동 횟수를 찾음
        # 이름 길이가 최대 20이므로, 각 위치를 기준으로 비교해도 충분히 빠름
        # 개선 코드: O(n^2)인데 n<=20 

# 사용 라이브러리 정리
    # ord() - 문자를 아스키 코드(정수)로 변환
        # 문제에서: 알파벳 상하 이동 횟수를 계산하기 위해 사용
    # min() - 여러 값 중 가장 작은 값 선택
        # 문제에서: 위/아래 이동 중 더 작은 값, 좌우 이동 후보 중 더 작은 값을 고르기 위해 사용
    # enumerate() - 인덱스와 값을 함께 순회
        # 문제에서: 각 위치와 알파벳을 같이 확인하기 위해 사용

def solution(name):
    answer = 0  # 전체 조작 횟수(상하 이동 + 좌우 이동)
    n = len(name)  # 이름 길이

    move = n - 1  # 일단 가장 단순한 경우인 '오른쪽으로만 끝까지 이동'을 최소 좌우 이동의 초기값으로 두고

    for i, char in enumerate(name): 
        # A에서 해당 문자까지 위/아래 이동 중 최소 횟수
        up_down = min(ord(char) - ord('A'), #위로
                      ord('Z') - ord(char) + 1) #아래로
      
        answer += up_down  # 각 자리의 상하 이동은 독립적이므로 바로 누적
        next_idx = i + 1  # 현재 위치 다음 칸부터 확인 시작

        while next_idx < n and name[next_idx] == 'A':
            next_idx += 1
        # 현재 위치 뒤에 A가 연속되는 구간이 어디까지인지 찾음
        # A는 이미 완성된 상태이므로, 이 구간을 건너뛰기

        move_right_then_left = i * 2 + (n - next_idx)
        # 오른쪽으로 i까지 갔다가 다시 왼쪽으로 되돌아온 뒤
        # 남은 뒤쪽 구간으로 가는 경우의 좌우 이동 수

        move_left_then_right = i + (n - next_idx) * 2
        # 반대로 뒤쪽 구간을 먼저 처리하고
        # 다시 돌아와 앞쪽을 처리하는 경우의 좌우 이동 수

        move = min(move, move_right_then_left, move_left_then_right)
        # 현재 위치를 기준으로 가능한 좌우 이동 후보들 중 가장 작은 값을 유지

    answer += move  # 최종 최소 좌우 이동 횟수를 더함

    return answer  # 전체 최소 조작 횟수 반환
